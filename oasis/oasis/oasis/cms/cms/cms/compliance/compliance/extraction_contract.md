Extraction contract:

Agent must:
- Extract explicitly documented data
- Label missing data as "not provided"
- Avoid inference
- Maintain section isolation
- Update only impacted sections when new documents are added
Additional enforcement:

- If weight-bearing status is not documented, state "not provided".
- If oxygen use is not documented, state "not provided".
- If any mobility domain is missing, flag a documentation gap.
