Extraction contract:

Agent must:
- Extract explicitly documented data
- Label missing data as "not provided"
- Avoid inference
- Maintain section isolation
- Update only impacted sections when new documents are added
